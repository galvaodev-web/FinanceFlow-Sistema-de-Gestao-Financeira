import { useState } from 'react'; import { LayoutDashboard, ArrowLeftRight, Tags, WalletCards, Menu, Wallet } from 'lucide-react'; import Dashboard from './pages/Dashboard'; import Transactions from './pages/Transactions'; import Categories from './pages/Categories'; import Budgets from './pages/Budgets';
const pages={dashboard:Dashboard,transactions:Transactions,categories:Categories,budgets:Budgets};const nav=[['dashboard','Dashboard',LayoutDashboard],['transactions','Transações',ArrowLeftRight],['categories','Categorias',Tags],['budgets','Orçamentos',WalletCards]];
export default function App(){const [page,setPage]=useState('dashboard'),[menu,setMenu]=useState(false);const Page=pages[page];return <div className="app"><aside className={menu?'open':''}><div className="brand"><span><Wallet size={19}/></span><b>Finance<span>Flow</span></b></div><nav>{nav.map(([id,label,Icon])=><button className={page===id?'active':''} key={id} onClick={()=>{setPage(id);setMenu(false)}}><Icon size={18}/><span>{label}</span></button>)}</nav><div className="profile"><i>GF</i><div><strong>Minha conta</strong><span>Finanças pessoais</span></div></div></aside>{menu&&<div className="scrim" onClick={()=>setMenu(false)}/>}<main><button className="mobile-menu" onClick={()=>setMenu(true)}><Menu size={20}/></button><Page/></main></div>}

